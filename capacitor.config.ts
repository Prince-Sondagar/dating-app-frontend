import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nextgencode.based',
  appName: 'Based',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
